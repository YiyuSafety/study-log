# 01-Python 文件读取与文本统计

## 一、今天学习目标

今天主要学习 Python 如何读取文本文件，并对文本内容进行简单统计。

本次练习完成了：

1. 使用 `pathlib` 定位文件路径
2. 使用 `read_text()` 读取文本内容
3. 使用 `len()` 统计字符数
4. 使用 `count()` 统计空格数
5. 使用 `splitlines()` 统计行数
6. 理解了 `\n` 换行符为什么会影响行数统计

---

## 二、练习文件位置

当前 Python 脚本位于：

```text
cyber-study/python/01文件的统计.py
